---
"wrangler-action": minor
---

Update default Wrangler version to v4 (4.72.0). The action now installs Wrangler v4 by default when no `wranglerVersion` input is specified. Users can still pin to v3 by setting `wranglerVersion: "3.90.0"` explicitly.
